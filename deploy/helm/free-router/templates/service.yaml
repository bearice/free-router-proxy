apiVersion: v1
kind: Service
metadata:
  name: {{ include "free-router.fullname" . }}
  labels:
    {{- include "free-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "free-router.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      appProtocol: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
